# Enable access for everyone
Require all granted

# Optional: allow directory listing (if you want it)
Options +Indexes

# Optional: ensure .htaccess itself can override configs
AllowOverride All
